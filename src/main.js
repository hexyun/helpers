import * as detector from './detector'
import * as rule from './validator/rule'
import Validator from './validator/Validator'
import * as control from './util/control'
export default {
  detector,
  rule,
  Validator,
  control
}
